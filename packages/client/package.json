{
	"name": "@spacemolt/client",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"check": "bun run strip-comments && bun run check:format && bun run check:lint && bun run check:test",
		"check:format": "prettier --write .",
		"check:lint": "tsc --noEmit",
		"check:test": "bun test",
		"start": "bun src/index.ts",
		"strip-comments": "bun ../../scripts/strip-comments.ts"
	},
	"dependencies": {},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
